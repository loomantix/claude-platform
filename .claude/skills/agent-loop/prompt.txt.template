Read @agent-loop-instructions.md and follow it. Your assigned issue is #{ISSUE_ID}. Run 'gh issue view {ISSUE_ID}' for the full description, then implement it, run focused validation, refactor, and create a local commit. Do not push, open a pull request, or invoke hosted AI reviewers (Gemini, Copilot, reviewit); the wrapper owns local Claude/Codex review, fresh-base integration, publication, and PR creation. If you must exit without a commit, first classify the bail and relabel the issue per the "Continuous improvement — classify every bail" section of the instructions (apply an agent-bail:<category> label, remove dev:agent, end your comment with the agent-loop-rca stub) — a silent bail teaches the system nothing.
